// Складіть програму, яка присвоює змінній h значення 5,
// а потім виводить на екран:
// у першому рядку - це значення з написом, 
// а в другому - квадрат цього значення (теж з пояснювальним написом).

let h = 5;
console.log(h, "добрий день еврібаді");
console.log(`добрий ${h * h}`);
